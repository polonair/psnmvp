<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>Bootstrap 101 Template</title>
        <link href="/bs337/css/bootstrap.min.css" rel="stylesheet">
        <link href="/xed/css/bootstrap-editable.css" rel="stylesheet">

        <!--[if lt IE 9]>
          <script src="https://oss.maxcdn.com/html5shiv/3.7.3/html5shiv.min.js"></script>
          <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
        <![endif]-->
    </head>

    <body>
        <nav class="navbar navbar-default navbar-static-top">
            <div class="container">
                <div class="navbar-header">
                    <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                        <span class="sr-only">Toggle navigation</span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                    </button>
                    <a class="navbar-brand" href="#">Project name</a>
                </div>
                <div id="navbar" class="navbar-collapse collapse">
                    <ul class="nav navbar-nav navbar-right">
                        {% if app.user %}
                            <li><a href="{{ path('profile') }}" >My profile</a></li>
                            <li><a href="{{ path('logout') }}">Exit</a></li>
                        {% else %}
                            <li><a href="{{ path('login') }}">Login</a></li>
                            <li><a href="{{ path('register') }}">Register</a></li>
                        {% endif %}
                    </ul>
                </div><!--/.nav-collapse -->
            </div>
        </nav>

        <div class="container">
            <h1>Profile info</h1>

            <table class="table">
                <tr><td>Gender</td><td>{{ ['', 'unknown', 'female', 'male'][link.profile.gender] }}</td></tr>
                <tr><td>Age</td><td>{{ link.profile.age }}</td></tr>
                {% if link.profile.activeSymptoms %}
                    <tr><td>Symptoms active</td><td>{{ link.profile.activeSymptoms }}</td></tr>
                {% endif %}
                {% if link.profile.pastSymptoms %}
                    <tr><td>Symptoms past</td><td>{{ link.profile.pastSymptoms }}</td></tr>
                {% endif %}
                {% if link.profile.possibleDiagnosis %}
                    <tr><td>Diagnosis Possible</td><td>{{ link.profile.possibleDiagnosis }}</td></tr>
                {% endif %}
                {% if link.profile.activeDiagnosis %}
                    <tr><td>Diagnosis Active</td><td>{{ link.profile.activeDiagnosis }}</td></tr>
                {% endif %}
                {% if link.profile.pastDiagnosis %}
                    <tr><td>Diagnosis Past</td><td>{{ link.profile.pastDiagnosis }}</td></tr>
                {% endif %}
                {% if link.profile.activeMedicaments %}
                    <tr><td>Medicaments Active</td><td>{{ link.profile.activeMedicaments }}</td></tr>
                {% endif %}
                {% if link.profile.pastMedicaments %}
                    <tr><td>Medicaments Past</td><td>{{ link.profile.pastMedicaments }}</td></tr>
                {% endif %}
                {% if link.profile.hospitals %}
                    <tr><td>Hospitals</td><td>{{ link.profile.hospitals }}</td></tr>
                {% endif %}
                {% if link.profile.doctors %}
                    <tr><td>Doctors</td><td>{{ link.profile.doctors }}</td></tr>
                {% endif %}
                {% if link.profile.procedures %}
                    <tr><td>Procedures</td><td>{{ link.profile.procedures }}</td></tr>
                {% endif %}
                {% if link.profile.alternativeMedicine %}
                    <tr><td>Alternative medicine</td><td>{{ link.profile.alternativeMedicine }}</td></tr>
                {% endif %}
                {% if link.profile.supplement %}
                    <tr><td>Supplement</td><td>{{ link.profile.supplement }}</td></tr>
                {% endif %}
            </table>
        </div> <!-- /container -->

        <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.12.4/jquery.min.js"></script>
        <script src="/bs337/js/bootstrap.min.js"></script>
    </body>
</html>
